Stability round after the first days of testing: importing a backup can no longer lose progress — your old data stays safe until the new file is fully verified. Adding rows now scrolls the new digits into view, music no longer restarts when you peek at notifications, and starting a new game always asks before discarding a running one. Plus many small fixes to double-taps, shared challenges, and the daily calendar.
